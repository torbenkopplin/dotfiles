# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# Example windowrules that are useful

windowrule {
    # Ignore maximize requests from all apps. You'll probably like this.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Fix some dragging issues with XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}

# Hyprland-run windowrule
windowrule {
    name = move-hyprland-run

    match:class = hyprland-run

    move = 20 monitor_h-120
    float = yes
}

layerrule = blur on,match:namespace waybar

# Float Necessary Windows
windowrule = float on, match:class battle.net
# windowrule = float on, match:class ^(org.pulseaudio.pavucontrol)
# windowrule = float on, match:class ^()$,title:^(Picture in picture)$
# windowrule = float on, match:class ^()$,title:^(Save File)$
# windowrule = float on, match:class ^()$,title:^(Open File)$
# windowrule = float on, match:class ^(LibreWolf)$,title:^(Picture-in-Picture)$
# windowrule = float on, match:class ^(blueman-manager)$
# windowrule = float on, match:class ^(xdg-desktop-portal-gtk|xdg-desktop-portal-kde|xdg-desktop-portal-hyprland)(.*)$
# windowrule = float on, match:class ^(polkit-gnome-authentication-agent-1|hyprpolkitagent|org.org.kde.polkit-kde-authentication-agent-1)(.*)$
# windowrule = float on, match:class ^(CachyOSHello)$
# windowrule = float on, match:class ^(zenity)$
# windowrule = float on, match:class ^()$,title:^(Steam - Self Updater)$
